# POC state — contains Confluent Cloud resource IDs, never commit
scripts/.poc-state

# Claude Code local settings — machine-specific, not project config
.claude/settings.local.json

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
.terraform.lock.hcl
bash.exe.stackdump
